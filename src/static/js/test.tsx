import * as ReactDOM from 'react-dom';
import * as React from 'react';

export default function hello() {
  console.log("if this works we're good to go");
}

// ReactDOM.render(React.createElement("h2", null, "Hello, world!"), document.body);